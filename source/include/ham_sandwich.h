// query data strutcures
#include "tokens.h"

#if !defined (_HAM_SANDWICH_H_)
    #define _HAM_SANDWICH_H_ 1
/*


    typedef struct queries_t{
        int length;
        int position;
        token_array_t *items;
    } queries_t;


    queries_t *  q_array     (unsigned length);
    void         q_close     (queries_t * q);
    void         q_push      (queries_t * q, token_array_t *item);
*/
#endif